# 04. Labels and Selectors Practice
**Keno use korchi?** Resource gulo-ke tag korar jonno Label use hoy ebong oi tag diye resource khuje ber korar jonno Selector use hoy.

**Commands:**
* **Apply:** `kubectl apply -f label-demo.yaml`
* **Select by Label:** `kubectl get pods -l env=production`
