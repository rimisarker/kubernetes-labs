apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-pvc-request
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: my-local-sc # এই নামটা উপরের StorageClass-এর সাথে মিলতে হবে
  resources:
    requests:
      storage: 500Mi
